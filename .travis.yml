sudo: false

language: java

jdk:
  - oraclejdk8

env:
  global:
    - MAVEN_OPTS="-Xms1024m -Xmx4096m -XX:PermSize=1024m"
    - JAVA_OPTS="-Xmx256m"
install:
 - mvn install

cache:
  directories:
  - $HOME/.m2

branches:
  only:
    - master
    - /^feature.*$/

   
notifications:
  email: false
  
script: mvn clean install

notifications:
  email: false
addons:
  sonarcloud:
    organization: "diniodinev-github"
    token:
      secure: "23d9f4bdb6610ca43090bc505153442f49bb3029"
    branches:
      - master
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner