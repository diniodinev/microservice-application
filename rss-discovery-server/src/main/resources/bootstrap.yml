spring:
  application:
    name: eureka-server
  cloud:
    config:
      failFast: false
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 5000
        maxAttempts: 20